<div>
  <% @providers.each do |provider| %>
  <li>
    <%= link_to provider.name, provider %>
  </li>
  <% end %>
</div>
</br>

<%= link_to t('actions.new'), new_provider_path, class: 'btn btn-primary btn-lg' %>
