<% if @sales.count != 0 %>
<table>
  <thead>
    <tr>
      <th>Produto</th>
    </tr>
  </thead>
  <tbody>
    <% @sales.each do |sale| %>
      <tr>
        <th><%= link_to sale.product.name, sale %></th>
      </tr>
    <% end %>
  </tbody>
</table>
<% end %>
